import { defineDefaultSlide, defineSlide } from '../utils/define-slide'

export default defineDefaultSlide({
  title: 'Data Store',
  content: (
    <div className="h-full w-full flex flex-col">
      <p>还记得之前我们使用外部状态库去管理数据吗？例如 Jotai，Zustand。</p>
    </div>
  ),
})
